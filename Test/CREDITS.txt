CREATE TABLE CREDITS(
CR_NUM STRING PRIMARY KEY,
CR_TYPE STRING,
CLIENT STRING,
SUM DECIMAL(28,12),
REPAID STRING,
CONSTRAINT CRTYPE FOREIGN KEY(CR_TYPE) REFERENCES CR_TYPES(CR_CODE),
CONSTRAINT CLI FOREIGN KEY(CLIENT) REFERENCES CLIENTS(CL_CODE)
);